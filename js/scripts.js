// Wait for DOM to be ready before executing scriptsdocument.addEventListener("DOMContentLoaded", function() {  // Update footer year automatically  const yearElement = document.getElementById("year");  if (yearElement) {    yearElement.textContent = new Date().getFullYear();  }  // Additional JavaScript for smoother carousel  const carousel = document.getElementById("booksCarousel");  if (carousel) {    const carouselInstance = new bootstrap.Carousel(carousel, {      interval: 5500,      pause: false,      ride: true    });// Enhanced scroll tracking for SEO metricslet maxScroll = 0;window.addEventListener("scroll", function() {  const scrollPercent = Math.round((window.scrollY /    (document.body.scrollHeight - window.innerHeight)) * 100);  if (scrollPercent > maxScroll) {    maxScroll = scrollPercent;    // Optional: Send to analytics    console.log("Max scroll depth:", maxScroll + "%");  }});